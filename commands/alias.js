module.exports = {
    name: 'alias',
    description: 'create and manipulate command aliases',
    execute(connection, args, guild, conf, locale, channel)
    {
        switch(args[1])
        {
            case "create":
                break;
            case "delete":
                break;
        }
    }
}